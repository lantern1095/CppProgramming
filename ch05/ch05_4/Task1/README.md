## 일반생성자와 복사생성자의 차이는?
- 일반생성자 : 객체를 처음 생성할때 호출된다. 사용자가 지정한 값으로 초기화한다.
- 복사생성자 : 객체를 처음 생성함과 동시에 원복 객체를 이용하며 생성할 때 호출, 원본 객체의 값으로 초기화

## 대입연산과 얕은 복사생성자의 차이는 무엇인가?
- 대입연산 : 이미 만들어진 객체에 값을 넣는 것
- 얕은 복사생성자 : 

## 지금까지 배운 내용기준으로 컴파일러가 자동으로 추가 하는 멤버함수는 어떤 것이 있는가?(4가지)
